spring.application.name=SIVA

# Conex�o
spring.datasource.url=jdbc:mysql://localhost:3306/ipem_db?createDatabaseIfNotExist=true&connectTimeout=30000
spring.datasource.username=root
spring.datasource.password=root

# Flyway
spring.flyway.enabled=true
spring.flyway.validate-on-migrate=false
spring.flyway.repair-on-migrate=true

# JPA
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true

# Seguran�a JWT (Evita o erro de inicializa��o do TokenService)
api.security.token.secret=${JWT_SECRET:3f0a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d2e3f4a5b6c7d8e9f0}